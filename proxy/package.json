{
  "name": "eve-gemini-proxy",
  "version": "0.1.0",
  "description": "Simple proxy to forward requests from browser to Gemini / Generative Language API to avoid CORS and protect API key.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "node-fetch": "^3.4.1",
    "dotenv": "^16.0.3"
  }
}
